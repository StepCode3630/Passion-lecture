meta {
  name: supprimer une catégorie
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/categories/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
