meta {
  name: mettre a jour  une catégorie
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/categories/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Roman modifié"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
