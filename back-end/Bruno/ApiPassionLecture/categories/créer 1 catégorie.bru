meta {
  name: créer 1 catégorie
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/categories
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Roman"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}