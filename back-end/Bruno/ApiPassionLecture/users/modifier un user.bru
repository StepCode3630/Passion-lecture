meta {
  name: modifier un user
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/users/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "fullName": "Stepan Modifié",
    "email": "step_modif@gmail.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
