meta {
  name: modifier un user
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/users/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "fullName": "Camille",
    "email": "camille@example.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
