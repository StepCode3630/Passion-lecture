meta {
  name: Se logout
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/user/logout
  body: none
  auth: bearer
}

params:query {
  : 
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
