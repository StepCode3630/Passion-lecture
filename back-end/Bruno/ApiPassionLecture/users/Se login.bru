meta {
  name: Se login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/user/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "Step@gmail.com",
    "password": "Qwertz1234."
  }
}

script:post-response {
  bru.setEnvVar("token", res.body.type.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
